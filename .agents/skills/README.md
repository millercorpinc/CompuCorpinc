# Repository Skills

This directory contains company-specific skills with names prefixed by `company-`.

The Superpowers installation scripts copy upstream skills into this same directory while preserving the company-specific folders.

Do not manually edit copied upstream skills. Change the pinned upstream version or contribute changes upstream instead.
